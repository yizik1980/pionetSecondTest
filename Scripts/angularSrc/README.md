# app test Library 

To run the demo serve please use json serve api

please run instllation ->

npm install -g json-server

After the installation ends in order to run it 
write down this  command

> # json-server --watch library.json